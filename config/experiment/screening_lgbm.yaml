# @package _global_
mode: "feature_screening" 
defaults:
  - override /model: lgbm
  - override /hparams: lgbm_screening
  - override /features: all
mlflow:
  experiment_name: "Feature_Screening_LGBM"
