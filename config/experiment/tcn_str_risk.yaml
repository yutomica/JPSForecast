# @package _global_
defaults:
  - /hparams/tcn/str_risk@hparams
  - /hparams/anchor@hparams: tcn_str_risk

preprocess:
  target_stratified_sampling:
    enabled: false
  sampling:
    enabled: true
    interval: 11
