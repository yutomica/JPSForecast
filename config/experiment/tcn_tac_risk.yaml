# @package _global_
defaults:
  - /hparams/tcn/tac_risk@hparams
  - /hparams/anchor@hparams: tcn_tac_risk

hparams:
  early_stopping_metric: src.models.custom_metrics.calc_ap_severe
  metric_direction: maximize

preprocess:
  target_stratified_sampling:
    enabled: true
    mode: mode_2
    center_keep_ratio: 0.2
    other_keep_ratio: 0.5
