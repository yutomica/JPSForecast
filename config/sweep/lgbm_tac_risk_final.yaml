# @package _global_
defaults:
  - override /hydra/sweeper: optuna
hydra:
  sweeper:
    # Optunaの設定
    sampler:
      _target_: optuna.samplers.TPESampler
      seed: 42
      n_startup_trials: 15
      multivariate: true
    direction: maximize  # Sharpe Ratioなどを最大化
    storage: "sqlite:///optuna.db"
    n_trials: 100       # 最適化のため試行回数を増加
    params:
      # 探索空間を広く定義し、ベイズ最適化（TPE）を適用
      hparams.feature_fraction: interval(0.1, 0.6)
      hparams.bagging_fraction: interval(0.5, 0.9)
      hparams.lambda_l1: tag(log, interval(0.01, 15.0))
      hparams.lambda_l2: tag(log, interval(0.01, 15.0))
      hparams.alpha: interval(0.05, 0.2)
      # 固定値
      hparams.verbose: -1